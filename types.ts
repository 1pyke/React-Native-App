export type IsLoginedIn = {
    loggedin?: boolean;
    Loading?: boolean;
  };
  export type LoginProps = {
    emailType: string;
    userNameType: string;
    passwordType: string;
  };
  export type UploadImage = {
    gallery:boolean;
  };